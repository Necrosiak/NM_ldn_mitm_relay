services:
  slp-server:
    image: spacemeowx2/slp-server-rust
    container_name: slp-server
    restart: unless-stopped
    ports:
      - "11451:11451/udp"
      - "11451:11451/tcp"
    networks:
      - lanrelay

networks:
  lanrelay:
    driver: bridge
